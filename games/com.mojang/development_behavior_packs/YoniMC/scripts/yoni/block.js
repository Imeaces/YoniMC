import default from "scripts/lib/yoni/basis.js";

//need more info

export default class Block {
  static objectIsBlock(object){
    let result = false;
    if (typeof object != "object")
      return result;
    
    return result;
  }
}

export { Block }
