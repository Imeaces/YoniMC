//bridge-file-version: #2
{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.test": {
      "initial_state": "default",
      "states": {
        "default": {
          "on_entry": [
            "/say 开始测试:molang:query.is_sleeping"
          ],
          "transitions": [
            {
              "loop": "query.is_sleeping"
            }
          ]
        },
        "loop": {
          "on_entry": [
            "/say 检测在到is_sleeping"
          ],
          "transitions": [
            {
              "default": "!query.is_sleeping"
            }
          ],
          "on_exit": [
            "/say 检测不是is_sleeping"
          ]
        }
      }
    }
  }
}