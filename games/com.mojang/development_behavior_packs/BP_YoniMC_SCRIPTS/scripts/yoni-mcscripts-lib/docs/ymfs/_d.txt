区块状态应该使用数组存储
因为实际测试下来，他妈的Map最慢，Set其次，Array反而读取的时候最快
但是在Array里查找是否有某一个元素，却是最慢的
~~所以存储被删除的区块的信息，应该用Set~~ 这段划掉
样本数 16*16*384*8


最后，已删除区块地址使用Set存储
使用Array存储未使用的区块，使用reverse ()后的数组，并使用pop()取出区块
是否需要存储已使用的区块？